{
  "name": "cafe-westen",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/main.ts --outdir=public/js --minify --sourcemap",
    "dev": "bun build src/main.ts --outdir=public/js --watch & bun run start:server",
    "start": "bun run build && bun run start:server",
    "start:server": "bun server.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "^1.30.0",
    "prettier": "^3.7.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@types/alpinejs": "^3.13.11",
    "alpinejs": "^3.15.2"
  }
}
